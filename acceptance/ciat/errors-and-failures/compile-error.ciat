To be run by the error compiler.
==== source
error
==== compilation
The "error" source will cause the compiler to error out.
==== execution
Nothing get executed because the compiler fails, so it doesn't matter what I write here.
